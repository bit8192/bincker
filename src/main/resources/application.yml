spring:
  application:
    name: bincker
  datasource:
    url: jdbc:sqlite:database.db
    driver-class-name: org.sqlite.JDBC
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
logging:
  level:
    cn.bincker: debug